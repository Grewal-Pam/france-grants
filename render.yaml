services:
  - type: web
    name: france-grants-streamlit
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements_streamlit.txt
    startCommand: streamlit run web/app.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
